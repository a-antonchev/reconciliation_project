[tool.ruff]
# длина строки (по умолчанию 88, как в black, но 120 для современных мониторов удобнее)
line-length = 120
target-version = "py311"

[tool.ruff.lint]
# включаем базовые правила (E - ошибки, F - логика, I - сортировка импортов)
select = ["E", "F", "I"]
